# assessment
APPROACHES TRIED
used claude ai to create a python script and tested it in vs studio. 
used claud ai to create a build123d script, it failed to created any code.
used chat gpt to create script and tried to resolve the errors using chat gpt.
tried to convert the tesselation of the stl file to make a solid body, then made a drawing form the given file and tried to auto-dimension it so that they can be added in chat gpt to create a better.
exported the stl file in step format and uploaded it in build123 to create script, then uploaded the script into chat gpt to convert it into stl type.
used the codes given in the build123d guide pdf and gave approximate dimensions.
used chat gpt to make the model step by step but code genereated by it gives errors.
the pdfs made in the process are uploaded in https://drive.google.com/drive/folders/1mkFoqFeZXU8CFFnlGrJa-7x01cVn4-8d?usp=drive_link
